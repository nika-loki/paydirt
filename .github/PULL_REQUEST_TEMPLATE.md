## What changes

<!-- User-visible summary; link the issue if one exists -->

## Checklist

- [ ] Offline tests pass: `for t in tests/*.sh; do bash "$t"; done`
- [ ] No secret value appears in any new output, example, or log path (placeholders only)
- [ ] Skill content changed in `plugins/gtm-sandbox/skills/` only (no per-tool duplication)
- [ ] If versions changed: both manifests + all three catalogs bumped in lockstep, changelog entry added
